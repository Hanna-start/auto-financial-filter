# 활성 스크리너(루트) 런타임 의존성 — engine.py + run_*.py + screen_us_liquidity.py
pandas>=1.5.0       # KR 시세 CSV 파싱 (run_kospi)
requests>=2.28.0    # KRX 거래일 조회 (run_kospi)
openpyxl>=3.0.0     # xlsx 산출
yfinance>=0.2.0     # 미국 시세 bulk

# 옛 '연간' 프레임워크(legacy/) 전용 의존성은 legacy/requirements.txt 참고.
